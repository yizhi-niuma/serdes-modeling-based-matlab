# Architecture

Reusable model implementations live under `src/`. Validation scripts mirror the
source module hierarchy under `validation/`. Fixed inputs live under `data/`, and
generated artifacts mirror the module hierarchy under `results/`.
